<%= form_with(model: hound, local: true) do |form| %>
  <% if hound.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hound.errors.count, "error") %> prohibited this hound from being saved:</h2>

      <ul>
      <% hound.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
