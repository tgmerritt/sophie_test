<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Sophie Test" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Sophie Test" %>">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <!-- reference the FaceMe SDK directly instead of using yarn / npm -->
    <script src="//unpkg.com/faceme-sdk/dist/umd"></script>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body style="margin:0">
    <!-- header>
      <%# render 'layouts/navigation' %>
    </header -->
    <%# <main role="main"> %>
       <%= render 'layouts/messages' %>
       <%= yield %>
    <%# </main> %>
  </body>
</html>
